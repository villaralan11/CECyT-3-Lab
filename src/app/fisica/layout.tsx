export const metadata = {
  title: "Física · Cinemática — CECyT No. 3 Lab",
  description: "Simuladores de MRU, MRUV y tiro parabólico con gráficas en vivo (DEMS Física I).",
};

export default function FisicaLayout({ children }: { children: React.ReactNode }) {
  return children;
}
