export const metadata = {
  title: "Mi progreso — CECyT No. 3 Lab",
  description: "Bitácora de avance: puntos, niveles y avance por tema. Exportable en JSON.",
};

export default function ProgresoLayout({ children }: { children: React.ReactNode }) {
  return children;
}
