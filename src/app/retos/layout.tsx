export const metadata = {
  title: "Retos rápidos — CECyT No. 3 Lab",
  description: "Quiz mixto de Física, Química e Inglés con feedback pedagógico. Suma puntos a tu bitácora.",
};

export default function RetosLayout({ children }: { children: React.ReactNode }) {
  return children;
}
