export const metadata = {
  title: "Química — CECyT No. 3 Lab",
  description: "Nomenclatura IUPAC, balanceo de ecuaciones y estequiometría con secuencia falla → simulador → ejemplo.",
};

export default function QuimicaLayout({ children }: { children: React.ReactNode }) {
  return children;
}
